init()
   prepareNextFigure() 
   putNextFigureInTheGlass() 
    loop()

loop()
    if (state.gameOver === 1)
        return finishGame()
    if (canCurrentFigureMoveDown())
        moveFigureDown()
    else
        putNextFigureInTheGlass()
        loop() with delay

//functions in order they callled in init and loop

prepareNextFigure()
    generateNextFigure()
    generatedNewFigure => state        

putNextFigureInTheGlass()
    isNextFigureCollideGlass()
        state.gameOver = 1
    currentFigure = nextFigure
    currentFigure.forEach => to the glass
    prepareNextFigure()

canCurrentFigureMoveDown()
    if (there are smth below currentFigure)
        return true (figure cant move down) 
    else 
        if (affective points belong figure)
        true; continue //keep on checking another points
        else 
            false //smth really below 
    
isNextFigureCollideGlass()
    state.nextFigure.forEach()
    return true
    
moveFigureDown()
    erase coords of currentFigure from the glass
    add 1 to Y of currentFigure coords
    update currentFigure state
    update glass state with coords of currentFigure

finishGame()
    state.gameState = 'GO'


TESTS

1. init()
    1.1. prepareNextFigure()
        T - log new generated figure, check state in vueDevTools

    1.2. putNextFigureInTheGlass()
    - isNextFigureCollideGlass()
        T - hardcode some points in the glass top and log the result of test
    - state.isGameOver = 1
        T - vueDevTools check state
    - currentFigure = nextFigure
        T - vueDevTools check state
    - currentFigure.forEach => to the glass
        T - test visually
    - prepareNextFigure()
        T - vueDevTools check state

2. loop()
    if (state.gameOver === 1)
         return finishGame()
         T - visually, and state via vueDevTools

    2.1. if (canCurrentFigureMoveDown())
         T - log coords of points which affects on logic of this test, and check are they correct
    
    2.2. moveFigureDown()
         T- visually
    else
        putNextFigureInTheGlass()
        T - above
        loop()
        T - visually



Phaze 2
horizontal movement

1. for small test
- keyevent handler for increase/decrease speed 
- use debounce to moderate increase/decrease functions

2. add horizontal movement
2.1. possibility of movement and obstacles
    - limit movement with glass walls
    - check occupied by other figures blocks 
        - check is figure selfaffect to this test result
2.2. movement
    - implement movement to 1 block for each keypress, w/o key-sticking
    - test in on a minimum speed with empty glass (obstacle - walls)
    - test it with semi-full glass (obstacles - walls+figures)
    - implement debounce function
    - test functional all together


rerender left

let GLASS_LIMIT_RIGHT = 
GLASS_LIMIT_LEFT 


MATRIX

1.    change figureStartCoords to be places in 0,0 coordinates
1.1.  for in game spawning the figure just add 4 to X of figureStartCoords

2.1.  getFigureStartCoords()
 // result is an array e.g. [[0, 1], [0, 2],[1, 0], [1, 1]]

2.2.  putCurrentFigureCoordsIn_IMAGINARY_Glass() (existing method with minor updates)
 // awaited result 
0, 1, 1
1, 1, 0
0, 0, 0
    - find center point

3.1.  transpositionGlassMatrix()
// rotation point according to this method always in the center, to prevent jumping right on each rotate
// by the way, square will also rotates around its bottom right block
// awaited result is an array looks like this
0, 1, 0
0, 1, 1
0, 0, 1

3.2.  getCoordsOfOccupiedBlocksIn_IMAGINARY_Glass()
// awaited result is an array e.g. [[0, 1], [1, 1],[1, 2], [2, 2]]

    - getCurrentFigureCoords()
    - find center point (or stricktly write in state )
    - build _IMAGINARY_ Rotated Figure around this point()
        - find differencies between X and Y of each block of currentFigure and its twin from zero-coords (3.2)
        - add results from prev. step to zero coords.
    - check collisions with actual glass()
if ok 
    - change currentFigureCoords()
    - change values in glass()



SHIFTING

1. - for each figure in state add 3 arrays of its block coords shift on every 90* rotate 

2.1. set Counter to 0
2.2. getCurrentFigureCoords() 
    - get it's name
    - get shift-model {counter+1}
    - check collisions of glass and shift-model
2.3. IF ok
    - setNewFigureCoords
    - Counter++
    - change values in glass()







